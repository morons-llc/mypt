{
  "name": "mypt",
  "version": "1.0.0",
  "description": "my project 2025",
  "main": "index.js",
  "scripts": {
    "build": "eleventy",
    "build:production": "rm -fr _site && npm run build",
    "deploy": "npm run build:production && firebase deploy",
    "dev": "eleventy --serve",
    "lint": "eslint",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@11ty/eleventy": "^2.0.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.8.0",
    "eslint": "^9.8.0",
    "eslint_d": "^14.0.3",
    "firebase-tools": "^13.15.0",
    "globals": "^15.9.0"
  }
}
