<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>My Project 2025</title>
        <link rel="stylesheet" type="text/css" href="/static/<%= staticFiles['main.css'] %>">
    </head>

    <body>
        <h1 class="font-bold">My Project 2025</h1>

        <ul>
            <% facts.forEach(fact => { %>
                <li><a href="/<%= fact.slug %>"><%= fact.slug %></a></li>
            <% }); %>
        </ul>
    </body>
</html>
